# 完整测试用例

以下测试用例覆盖后端、前端、调度、AI 报告、评分、异常场景与变更联动检查。

## 1. 后端接口测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `BE-001` | 健康检查 | 服务正常启动 | 服务已启动 | 调用 `GET /api/v1/health` | 返回 `code=0` 与 `status=up` |
| `BE-002` | 实时价格 | 存在最新 tick 数据 | 数据库存在最新价格 | 调用 `GET /api/v1/prices/realtime` | 返回最新 `CNY/g` 价格 |
| `BE-003` | 实时价格 | 无价格数据 | 清空价格表 | 调用 `GET /api/v1/prices/realtime` | 返回空数据或明确错误码 |
| `BE-004` | 历史走势 | 查询 1 日分时数据 | 已生成 `1m` K 线 | 调用 `GET /api/v1/prices/history?range=1d&interval=1m` | 返回升序时间序列 |
| `BE-005` | 历史走势 | 非法周期参数 | 服务正常 | 调用非法 `range` | 返回参数错误 |
| `BE-006` | 新闻列表 | 分页查询 | 已有新闻数据 | 调用 `/news?page=1&page_size=20` | 返回分页结果 |
| `BE-007` | 新闻详情 | 查询存在的新闻 | 已有新闻 ID | 调用 `/news/:id` | 返回对应新闻详情与正文 |
| `BE-008` | 新闻详情 | 查询不存在新闻 | 服务正常 | 调用不存在 ID | 返回 `4004` |
| `BE-009` | 因子面板 | 获取最新因子 | 已有 10 个因子快照 | 调用 `/factors/latest` | 返回 10 个因子最新值 |
| `BE-010` | 因子历史 | 查询单因子历史 | 已有 `usd_index` 数据 | 调用 `/factors/history?code=usd_index&range=30d` | 返回时间序列 |
| `BE-011` | 报告列表 | 查询报告列表 | 已有报告数据 | 调用 `/reports` | 返回分页列表 |
| `BE-012` | 报告详情 | 查看报告详情 | 已有报告 ID | 调用 `/reports/:id` | 返回完整报告与评分 |
| `BE-013` | 准确率曲线 | 查询最近 90 天 | 已有评分数据 | 调用 `/reports/accuracy/curve?range=90d` | 返回曲线点位和均分 |
| `BE-014` | 手动触发任务 | 触发金价采集 | 后台接口可用 | 调用 `POST /admin/jobs/collect-price` | 创建 `job_runs` 记录 |
| `BE-015` | 手动触发任务 | 触发报告生成 | AI 配置有效 | 调用 `POST /admin/jobs/generate-report` | 报告成功入库 |

## 2. 数据采集测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `DA-001` | 金价采集 | 主数据源可用 | 配置主源 | 执行采集任务 | 成功入库最新 tick |
| `DA-002` | 金价采集 | 主源失败切换备用源 | 配置 `remote + mock` | 模拟主源不可用 | 自动使用备用源并继续返回价格 |
| `DA-003` | 金价采集 | 返回异常值 | 模拟异常高价或异常跳变 | 执行采集 | 异常值被拦截且不入库 |
| `DA-004` | 金价采集 | 单位转换 | 原始数据非 `CNY/g` | 执行采集 | 成功统一成 `CNY/g` |
| `DA-005` | 新闻抓取 | 新闻去重 | 数据库存在同新闻 | 再次抓取 | 不重复入库 |
| `DA-006` | 新闻抓取 | 缺少正文 | 抓取源缺正文 | 执行抓取 | 仍可保存标题和摘要占位 |
| `DA-009` | 新闻抓取 | 分类和地区映射 | 输入包含宏观或地缘关键词 | 执行抓取 | 自动生成 `category` 和 `region` |
| `DA-010` | 新闻抓取 | 关联因子映射 | 输入包含美元或地缘关键词 | 执行抓取 | `related_factors_json` 写入对应因子 |
| `DA-007` | 因子采集 | 美元指数更新 | 配置宏观源 | 执行因子更新 | 生成 `usd_index` 快照 |
| `DA-008` | 因子采集 | 地缘政治事件型因子 | 有相关新闻事件 | 执行因子更新 | 生成事件型评分快照 |

## 3. AI 报告与评分测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `AI-001` | 日报生成 | 输入数据完整 | 当日价格、新闻、因子齐全 | 运行日报任务 | 生成一份完整报告 |
| `AI-002` | 日报生成 | 输入数据部分缺失 | 缺少部分新闻 | 运行日报任务 | 报告可降级生成并标记缺失 |
| `AI-003` | 日报生成 | AI 超时 | 模拟 AI 请求超时 | 运行日报任务 | 任务失败并记录错误 |
| `AI-004` | 结构化预测 | 解析报告方向与区间 | 报告已生成 | 执行结构化解析 | 写入 `report_predictions` |
| `AI-005` | 报告评分 | 次日方向预测正确 | 存在前一日报告 | 执行评分 | 方向得分高 |
| `AI-006` | 报告评分 | 次日方向预测错误 | 存在前一日报告 | 执行评分 | 方向得分低 |
| `AI-007` | 报告评分 | 区间命中 | 预测区间覆盖真实值 | 执行评分 | 区间得分高 |
| `AI-008` | 报告评分 | 风险提示命中 | 实际发生风险事件 | 执行评分 | 风险得分增加 |
| `AI-009` | 报告评分 | 重复评分保护 | 评分已存在 | 再次执行评分 | 阻止重复或执行幂等更新 |

## 4. 定时任务测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `CRON-001` | 价格采集任务 | 按周期执行 | cron 已启用 | 等待调度 | 周期性写入 tick |
| `CRON-002` | 新闻任务 | 按周期执行 | cron 已启用 | 等待调度 | 周期性抓取新闻 |
| `CRON-003` | 报告任务 | 每日执行 | 已配置每日时间 | 等待触发 | 当日报告自动生成 |
| `CRON-004` | 评分任务 | 次日执行 | 有昨日报告 | 等待触发 | 自动评分成功 |
| `CRON-005` | 任务失败重试 | 采集失败 | 启用重试机制 | 模拟失败 | 任务按规则重试 |

## 5. 前端页面测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `FE-001` | 首页 | 首屏加载 | 接口正常 | 打开首页 | 价格、因子、新闻、报告摘要正常显示 |
| `FE-002` | 价格页 | 切换周期 | 图表接口正常 | 切换 `1D/7D/30D` | 图表正确刷新 |
| `FE-003` | 价格页 | SSE 实时刷新 | SSE 正常 | 保持页面打开 | 最新价格自动更新 |
| `FE-004` | 新闻页 | 按因子筛选 | 数据存在 | 选择因子标签 | 列表按条件过滤 |
| `FE-005` | 因子页 | 查看因子详情 | 因子历史存在 | 点击因子卡片 | 展示因子历史曲线 |
| `FE-006` | 报告页 | 查看报告详情 | 有历史报告 | 点击列表项 | 展示完整报告与评分 |
| `FE-007` | 准确率曲线 | 显示历史评分 | 有评分数据 | 打开报告页 | 准确率曲线正常绘制 |
| `FE-008` | 异常态 | 接口失败 | 模拟接口错误 | 打开页面 | 显示错误态与重试入口 |
| `FE-009` | 空态 | 无新闻数据 | 清空新闻表 | 打开新闻页 | 显示空态占位 |
| `FE-010` | 响应式 | 移动端浏览 | 浏览器切移动端 | 打开核心页面 | 布局不重叠、图表可读 |

## 6. 性能与稳定性测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `PF-001` | 历史图表 | 1 年数据查询 | 已有大量 K 线 | 查询 `1y` | 接口在可接受时间内返回 |
| `PF-002` | 新闻列表 | 大数据分页 | 已有 1 万条新闻 | 查询分页 | 响应稳定 |
| `PF-003` | 准确率曲线 | 长时间序列 | 已有 365 天评分 | 查询曲线 | 曲线接口稳定返回 |
| `PF-004` | 定时任务 | 连续运行 24 小时 | 调度已开启 | 观察日志 | 无明显内存泄漏或任务堆积 |

## 7. 安全与容错测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `SE-001` | 参数安全 | 传入非法分页参数 | 服务正常 | 调用列表接口 | 返回参数错误 |
| `SE-002` | 后台任务 | 未授权调用 | 启用鉴权 | 调用后台接口 | 返回未授权 |
| `SE-003` | AI 内容 | 模型返回空文本 | 模拟异常响应 | 生成报告 | 任务失败并记录日志 |
| `SE-004` | 数据源 | 外部源不可达 | 模拟断网 | 执行采集 | 任务失败但服务不崩溃 |

## 8. 变更同步验证测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `DOC-001` | 文档联动 | 修改数据库模型 | Git 可用 | 运行 `make check-sync` | 若未改表结构文档则校验失败 |
| `DOC-002` | 接口联动 | 修改 API Handler | Git 可用 | 运行 `make check-sync` | 若未改 API 文档则校验失败 |
| `DOC-003` | 版本联动 | 修改核心功能 | Git 可用 | 运行 `make check-sync` | 若未改 `CHANGELOG.md` 则失败 |
| `DOC-004` | 测试联动 | 修改业务逻辑 | Git 可用 | 运行 `make check-sync` | 若未改测试用例文档则失败 |

## 9. 建议测试执行顺序

1. 后端接口测试
2. 数据采集测试
3. AI 报告与评分测试
4. 前端页面测试
5. 性能与稳定性测试
6. 变更同步验证测试

## 10. 当前首版骨架补充测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `BOOT-001` | 后端启动 | SQLite 自动建表 | 已执行 `go mod tidy` | 运行 `make run-backend` | 服务启动且生成数据库文件 |
| `BOOT-002` | 后端接口 | 首页概览接口 | 服务已启动 | 调用 `/api/v1/dashboard/overview` | 返回 mock 大盘数据 |
| `BOOT-003` | 前端启动 | Vite 首页渲染 | 已执行 `npm install` | 运行 `npm run dev` | 页面正常加载无白屏 |
| `BOOT-004` | 图表渲染 | 价格图与准确率图 | 前后端同时启动 | 打开首页 | 两张 ECharts 图表正常展示 |
| `BOOT-005` | 跨域 | 前端访问后端 API | 前后端同时启动 | 打开首页并观察请求 | API 请求成功，无 CORS 错误 |
| `BOOT-006` | 新闻预热 | 首次启动自动抓取新闻 | 空数据库启动服务 | 启动后调用 `/news` | 返回预热后的新闻列表 |

## 11. 本轮价格持久化补充测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `PRICE-001` | 价格采集 | 启动时历史预热 | 空数据库启动服务 | 启动后查询 `/prices/history?range=1d&interval=1m` | 返回数据库中的分钟级数据 |
| `PRICE-002` | 价格采集 | 手动触发采集 | 服务已启动 | 调用 `POST /admin/jobs/collect-price` | 新增一条 tick 与 job run |
| `PRICE-003` | K 线聚合 | 自动更新分钟与小时 K 线 | 服务已启动 | 连续触发多次采集 | `gold_price_candles` 出现对应窗口数据 |
| `PRICE-004` | 数据回读 | 实时价格读库 | 数据库已有 tick | 调用 `/prices/realtime` | 返回最近一次持久化价格 |
| `PRICE-005` | 定时采集 | 周期自动执行 | 设置 `PRICE_COLLECT_INTERVAL_SEC` | 等待一个周期 | 数据库新增采集记录 |
| `PRICE-006` | 主备切换 | 主源不可用时回退 | 配置 `GOLD_SOURCE_MODE=remote` 且主源失败 | 调用采集任务 | 价格由备用源返回并成功入库 |
| `PRICE-007` | 数据清洗 | 历史预热存在异常点 | 启动时返回异常历史序列 | 执行预热 | 异常点被过滤，剩余有效点成功入库 |
| `PRICE-008` | SSE 推送 | 建立持续连接 | 服务已启动 | 连接 `/prices/stream` 并等待 | 收到 `price_status` 和连续 `price_tick` 事件 |

## 12. 本轮新闻系统补充测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `NEWS-001` | 新闻抓取 | 手动触发抓取 | 服务已启动 | 调用 `POST /admin/jobs/fetch-news` | 新增新闻与 `job_runs` 记录 |
| `NEWS-002` | 新闻去重 | 重复抓取同一批新闻 | 数据库已有新闻 | 再次调用抓取接口 | 仅新增 `job_runs`，新闻不重复入库 |
| `NEWS-003` | 新闻筛选 | 按分类筛选 | 已有多类别新闻 | 调用 `/news?category=geopolitics` | 仅返回指定类别 |
| `NEWS-004` | 新闻筛选 | 按因子筛选 | 已有关联因子数据 | 调用 `/news?factor_code=usd_index` | 仅返回包含该因子的新闻 |
| `NEWS-005` | 新闻详情 | 获取正文详情 | 已有新闻数据 | 调用 `/news/:id` | 返回 `content`、`summary`、`source_name` |

## 13. 本轮因子系统补充测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `FACTOR-001` | 因子预热 | 首次启动自动初始化 | 空数据库启动服务 | 启动后调用 `/factors/definitions` | 返回 10 个预置因子定义 |
| `FACTOR-002` | 因子预热 | 自动生成历史快照 | 空数据库启动服务 | 启动后调用 `/factors/history?code=usd_index&range=90d` | 返回按时间升序的历史快照 |
| `FACTOR-003` | 因子更新 | 手动触发最新快照生成 | 服务已启动 | 调用 `POST /admin/jobs/update-factors` | 新增 10 条 `factor_snapshots` 与一条 `job_runs` |
| `FACTOR-004` | 因子面板 | 首页真实读库 | 数据库已有因子快照 | 调用 `/factors/latest` | 返回 10 个因子的最新数据库值，而非 mock 数据 |
| `FACTOR-005` | 因子规则 | 地缘新闻推升避险因子 | 存在地缘正向新闻 | 执行因子更新 | `geopolitics` 与 `safe_haven_sentiment` 分值上升且为正 |

## 14. 本轮报告与评分系统补充测试

| 用例 ID | 模块 | 场景 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| `REPORT-001` | 报告生成 | 手动生成指定日期日报 | 服务已启动 | 调用 `POST /admin/jobs/generate-report` 并传 `report_date` | 新增 `analysis_reports` 和 `report_predictions` |
| `REPORT-002` | 报告生成 | 启动自动预热历史日报 | 空数据库启动服务 | 启动后调用 `/reports` | 返回近 30 天分页列表 |
| `REPORT-003` | 报告详情 | 查看结构化预测与评分 | 报告已生成并评分 | 调用 `/reports/:id` | 返回 `predictions` 和 `score` 详情 |
| `REPORT-004` | 报告评分 | 手动评分指定日报 | 已存在指定日报 | 调用 `POST /admin/jobs/score-report` 并传 `report_date` | 新增或覆盖 `report_scores` |
| `REPORT-005` | 准确率曲线 | 历史评分真实读库 | 数据库已有历史评分 | 调用 `/reports/accuracy/curve?range=30d` | 返回数据库中的评分曲线和均分 |
